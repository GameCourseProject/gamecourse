rule: Initial Bonus
# Attributed to everyone in the beginning of course

    when:
        bonus = METADATA["initial_bonus"]
        logs = get_logs(target, "initial bonus")
        len(logs) == 1
		
    then:
        award_bonus(target, "Initial Bonus", bonus)