/**
 * This is the configuration file template. Make a copy of it in the same
 * directory, rename it to config.ts and fill-in the necessary information.
 */

export const config = {
  API_URL: '<API_URL>',
};
